<?xml version="1.0" encoding="utf-8"?>
<!-- REDESIGNED: Material You Sender Message Bubble -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="end"
    android:paddingHorizontal="12dp"
    android:paddingVertical="3dp">

    <!-- Message bubble + timestamp column -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginEnd="8dp"
        android:gravity="end"
        android:orientation="vertical">

        <!--
            FIX: MaterialCardView does NOT support cardCornerRadiusBottomEnd etc.
            Use shapeAppearanceOverlay to set per-corner radii:
              topStart=20dp, topEnd=20dp, bottomStart=20dp, bottomEnd=4dp (sharp tail)
        -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:cardElevation="0dp"
            app:cardBackgroundColor="?attr/colorPrimaryContainer"
            app:strokeWidth="0dp"
            app:shapeAppearanceOverlay="@style/ShapeAppearance.ChitChat.SenderBubble">

            <TextView
                android:id="@+id/msgsendertyp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/poppins_regular"
                android:maxWidth="240dp"
                android:padding="10dp"
                android:paddingStart="14dp"
                android:paddingEnd="14dp"
                android:text="Message text"
                android:textColor="?attr/colorOnPrimaryContainer"
                android:textSize="14sp" />

        </com.google.android.material.card.MaterialCardView>

        <!-- Timestamp + seen indicator -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:layout_marginTop="2dp"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/msgTimestamp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/poppins_regular"
                android:text="12:34"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:textSize="10sp" />

            <ImageView
                android:id="@+id/seenIndicator"
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:layout_marginStart="3dp"
                android:src="@drawable/ic_done_all"
                app:tint="?attr/colorPrimary"
                android:visibility="gone" />

        </LinearLayout>

    </LinearLayout>

    <!-- Sender avatar -->
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/profilerggg"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_gravity="bottom"
        android:src="@drawable/photocamera"
        app:civ_border_color="?attr/colorPrimary"
        app:civ_border_width="1.5dp" />

</LinearLayout>